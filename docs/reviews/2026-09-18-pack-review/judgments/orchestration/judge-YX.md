### Steelman Y

Y offers two self-contained items that split orchestration into its two natural phases. **item-4b5c699b** gates all scaffolding work behind a mandatory "Fit test" and a seven-fact interview conducted "in one batch," refuses to run anything itself ("Do not run the sweep here: scaffolding only"), and requires the manifest to be generated by actually running the enumeration and committed before any worker runs — a genuine plan-then-stop gate on consequential setup work. **item-bdc9b27e** is a read-only supervisor whose `disallowedTools` frontmatter mechanically blocks Write/Edit/NotebookEdit, so its "don't touch the loop" rule is enforced at the tool layer, not just in prose. It names four precise, falsifiable failure conditions (Stall, Retry storm, Cost drift, Blocked queue), requires every recommendation to carry the evidence that produced it ("quote the before and after," "quote the two checkpoints or the repeated failure text"), mandates explicit **Checked**/**Not checked** sections after every report, and hands resume authority to a human by rule ("Resuming after a pause is the human's call"). Together the pair covers setup-gating and in-flight supervision/escalation with concrete, falsifiable checklists and one real mechanical restriction.

### Steelman X

X is a single, deliberately thin discovery stub that avoids baking a large, potentially stale orchestration guide into context: it resolves the correct CLI binary once (with real-world edge cases handled, like not letting a bare `orca` on Linux accidentally launch the GNOME screen reader), then fetches a version-matched usage guide from that binary at runtime rather than duplicating instructions that could drift out of sync with the tool itself. Its rules are concrete and safety-conscious for the narrow job it does — "report its exact error and stop. Do not fall through to another executable," "do not guess unsupported commands" — and it correctly routes different needs (coordination vs. full-ownership handoff) to different guides/skills, suggesting a broader, more modular orchestration toolset than what's visible in this one file.

### Scores

| Criterion | Y | X |
|---|---|---|
| Fit with the bar | 3 — item-bdc9b27e explicitly reinforces all three behaviors (mandatory Checked/Not-checked, evidence-quoting recommendations, human-only resume); item-4b5c699b reinforces plan-then-stop and partial evidence, with checked/not-checked only "mostly ignored," never contradicted. | 1 — report calls plan-then-stop only "partial support" and states evidence-before-done and checked/not-checked are "not covered" for this item, leaving two of three behaviors unaddressed rather than reinforced. |
| Enforcement mechanism | 2 — item-bdc9b27e's `disallowedTools` frontmatter is a real tool-layer block ("fails closed at the tool layer"); item-4b5c699b's checklist is prose-only and "fails open if the checklist is skipped." | 0 — report states plainly "no script, hook, or exit-code check in this item itself... nothing in the file itself can block or verify." |
| Context cost | 2 — both load on demand; item-bdc9b27e is self-contained with no bundled files, but item-4b5c699b bundles six template files plus `references/doctrine.md` that load with it. | 2 — loads on demand and the stub's own quoted content is short, but it necessarily triggers a further runtime fetch of an uncounted, version-dependent external guide. |
| Maintenance burden | 3 — report states item-bdc9b27e names "no runtime, CLI, or service of its own," and item-4b5c699b depends only on its own bundled templates plus an existing subagent-dispatch capability. | 0 — the skill is inert without a separately installed, version-matched external "orca"/"orca-dev"/"orca-ide" binary, which the report treats as a functional hard dependency despite the file's own "runtime deps: none named." |
| Specificity | 3 — item-4b5c699b's seven-fact interview and five scaffold rules, and item-bdc9b27e's four named failure modes with precise detection criteria ("no change... across two consecutive checkpoints"), are concrete and falsifiable. | 1 — concrete only about CLI-resolution edge cases (screen-reader collision, fallback rules); the report states the substantive orchestration mechanics (dispatch, dependency waits, escalation, DAGs) are "not present in this file." |

### Per-item rows

| Item | Class | Note |
|---|---|---|
| item-4b5c699b | COMPLEMENT | Fills a gap X's set does not address: an interview-gated scaffolding phase that generates a committed manifest and per-item state before any batch work runs. |
| item-bdc9b27e | COMPLEMENT | Fills a gap X's set only defers to an unseen runtime-fetched guide: verifiable, mechanically-enforced (tool-blocked) loop supervision with mandatory evidence and Checked/Not-checked reporting. |
| item-5f05bf4c | COMPLEMENT | Fills a gap neither Y item addresses: safe discovery and version-matching of an external orchestration CLI binary, with routing to a separate full-ownership handoff skill. |

### Deciding criteria

Specificity and fit with the bar: the three items address non-overlapping sub-problems (scaffolding, in-flight supervision, external-tool discovery) at very different depths of concrete, falsifiable detail, so none does the same job as another and no SUPERSEDES/REDUNDANT pairing applies.

### What I could not assess from reading alone

A behavioral test would need to run the generated `/sweep` harness from item-4b5c699b and item-bdc9b27e's supervision against a real stuck loop to see whether the checklists are actually followed rather than skipped, since both rely on prose self-discipline for their core judgments. For X, the decisive unknown is the content of the guide fetched by `ORCA skills get orchestration` at runtime — the report explicitly could not see it, so whether X's actual orchestration behavior (dispatch, dependency waits, escalation, DAGs) satisfies the three-behavior bar at all is unverifiable from this file alone; that guide's absence is also why X scores low here, not necessarily because the real toolset lacks it. I did not form a confident guess about either candidate's source and did not attempt to.
