### Steelman

The set is deliberately layered rather than duplicative: two skills (`7aed43c3`, `92c76be2`) split "what counts as evidence" from "how to write it up," two more (`24501ddb`, `e739493a`) each back a specific claim type (an artifact is live; a change is safe to apply) with a real fail-closed script, and the agent (`06c31a80`) sits at the end as a non-repairing, non-modifying pre-delivery gate that explicitly refuses "fix it and re-verify." Every item loads on demand against a stated trigger rather than sitting always-on, three of the five items ship an actual script rather than prose (`check-poison-coverage.py`/`generate-smoke-script.py`, `verdict-check.sh`, and the agent's `disallowedTools` block), and the weakest points are self-disclosed inside the report rather than papered over — e.g. `24501ddb` states its own Stop-hook is "optional... and not proven by a fixture," `e739493a` states its validator "cannot check that the reviewer actually looked at the change," and `06c31a80` cites a prior incident where an identically-restricted sibling was defeated. That candor is itself evidence the set was built to be relied on, not merely to look complete.

### Scores

| Item | Fit with the bar | Enforcement mechanism | Context cost | Maintenance burden | Specificity |
|---|---|---|---|---|---|
| item-7aed43c3 | 3 — reinforces "executed evidence" and "not-verified list" behaviors, no conflict noted | 1 — report states plainly "no script or hook in this item enforces any of it" | 3 — triggers only "before declaring any artifact complete" | 3 — "None named at runtime... produces no file, log, or artifact of its own" | 3 — names an ordered fallback sequence (build→types→lint→tests→secrets→diff) with hard stops |
| item-92c76be2 | 2 — strong on "say what was checked" but "takes CHECK/OUTPUT on faith from whoever invokes it" | 0 — "no script validates that a written report actually satisfies the four-field shape" | 3 — triggers only for "presenting verification results... or any claim that work is done" | 3 — "None of its own... assumes checks were already run elsewhere" | 3 — fixed four-field block, verbatim-OUTPUT rule, ban on additive improvement math |
| item-24501ddb | 3 — bars "a live exit 0 with fewer poison categories proven than the manifest declares" | 3 — two scripts fail closed and an "offline fixture-proof script exercises this whole chain" | 2 — on-demand but pulls in a five-category manifest, hook wiring, and env vars | 1 — needs "bash, python3, and PyYAML" plus "a Browser-automation tool or Playwright for a real screenshot" | 3 — five named categories each with a required poison value and exact exit-code contract |
| item-e739493a | 3 — gates apply behind a passing verdict with named stop conditions (missing goal block, malformed verdict, same-model reviewer, repeat fail) | 3 — `verdict-check.sh` "fails closed" and prints `HOLD` on a second unconvincing fail | 3 — triggers only "before applying any change spec produced by a bounded loop," explicitly not for plain review | 2 — "the item stops if none is configured" rather than needing install, but does require a differently-modeled subagent | 3 — concrete verdict schema (result/severity/confidence/issues ranges) and hold logic |
| item-06c31a80 | 2 — evidence/verdict discipline strong, but its central "never modifies a file" guarantee is admittedly a "charter," not a checked boundary | 1 — its own text calls the `disallowedTools` block "a narrowed surface, not a boundary" and cites a documented bypass of an identical restriction | 3 — triggers "when work is about to be presented as done" | 3 — "None — it never writes files by design" | 3 — exactly three named verdicts, explicit ban on "looks fine"-style language |

### Per-item rows

| Item | Class | Criterion | Note |
|---|---|---|---|
| item-7aed43c3 | TIGHTEN Enforcement | Enforcement mechanism | A tighter version would ship a script that confirms the fallback sequence actually ran (captures build/type/lint/test/secret exit codes) rather than trusting the agent to follow the prose sequence. |
| item-92c76be2 | TIGHTEN Enforcement | Enforcement mechanism | A tighter version would validate the four-field shape and the not-verified list mechanically, and flag a CHECK/OUTPUT pair that cites no traceable run instead of formatting it on faith. |
| item-24501ddb | TIGHTEN Stop-hook | Enforcement mechanism | The report itself says the Stop-hook that would make a red result unavoidable is "optional... and not proven by a fixture"; a tighter version makes the hook mandatory and covers it with the same fixture-proof script used for the rest of the chain. |
| item-e739493a | TIGHTEN verdict evidence field | Fit with the bar | Per-issue `evidence` can be "a quote under 15 words" rather than run output, which is why the item concedes its validator "cannot check that the reviewer actually looked at the change"; a tighter field would require a reference to an actual executed check. |
| item-06c31a80 | TIGHTEN Enforcement | Enforcement mechanism | Its non-modification guarantee rests on a `disallowedTools` list it calls "a narrowed surface, not a boundary," with a cited precedent of an identical restriction being defeated via heredoc; a tighter version would need a hook or sandbox that inspects Bash commands for write patterns rather than relying on the agent's own charter. |

### Deciding criteria

Enforcement mechanism decided four of the five rows; item-e739493a's row turned on Fit with the bar because its enforcement script is real but validates the wrong thing (verdict shape, not evidence substance).

### What I could not assess from reading alone

Whether the fallback check sequence in `7aed43c3` is actually invoked and followed when a project names no sequence of its own; whether `92c76be2` in practice gets fed fabricated CHECK/OUTPUT text without detection; whether `24501ddb`'s optional Stop-hook, once wired into a real `settings.json`, actually blocks a turn on a red result (the report says this is untested by its own fixture); whether `verdict-check.sh` correctly fails closed against real, not just fixture, malformed verdicts; and most importantly, whether `06c31a80`'s non-modification "charter" holds under adversarial pressure — the report cites a prior incident where an identically-restricted sibling agent was made to rewrite a file via heredoc, so this specific failure mode would need a live red-team run against this exact agent to know if it recurs.
