Confirmed — these are Node test suites exercising the two validator scripts. I have full coverage of all files now and can write the report.

### Items

| id | type | one line |
|---|---|---|
| item-fd563265 | skill | A defensive, source-first security-audit skill: guidance mode for ad-hoc questions/reviews, and a full six-phase multi-agent audit workflow (recon → coverage-led hunting → candidate validation → structured findings.json → independent record verification → report) with a sandboxed local-execution model, a coverage ledger, and two Node.js schema validators plus their test suites. |

### For each item

**item-fd563265**
**Trigger:** Loads via its `name`/`description` frontmatter (a skill-discovery mechanism) whenever the request concerns security questions, focused reviews, vulnerability research, audits, or pen tests; the file states loading it "does not authorize the complete audit workflow or file creation." The full six-phase workflow and file writes trigger only on an explicit ask to audit/pen-test a codebase, a full/comprehensive/end-to-end review, or a request for report artifacts; otherwise it operates in a lighter "guidance mode." Not always-on for the heavy workflow — it is agent-neutral (calls it "Parent," "Task tool," `research`/`general` delegated agents) and defers to whichever delegation mechanism the host platform provides.
**What it makes the agent do:** In full-audit mode: (1) resolve target/output-dir/source-ref, gate on a strict cost budget before any agent launch; (2) Phase 1 recon via several parallel read-only sub-agents that map principals, entry surfaces, and build a deterministic "coverage ledger" (structured JSON, one unit per surface×boundary×subsystem×attack-class combination, each with a `coverage_id` derived by an explicit percent-encoding/canonicalization algorithm); (3) Phase 2 assigns hunter sub-agents to ledger units guided by a large library of domain-specific attack-class checklists (injection, access control, business logic, AI/LLM, web/auth, client-side, cloud, protocols, memory-safety, supply-chain, resource-exhaustion, data-isolation, desktop/IPC), each carrying explicit "validation rules" that require naming a lower-trust principal, a crossed boundary, and a concrete observed result — "prompt injection alone is not a finding," "a missing rate limit is not enough," etc.; hunters must run any target-controlled code only inside an OS-enforced sandbox with no network, an empty allowlisted environment, read-only target, scratch-only writes, and explicit resource caps — "if any control is unavailable, do not execute: return needs_validation with that exact blocker"; (4) Phase 3 gives every candidate to a fresh, independent verifier who "must re-read every cited current source location" and try to refute it; (5) Phase 4 writes `findings.json` and runs the two bundled validator scripts, refusing to continue on any structural/semantic error; (6) Phase 5 launches a second, independent fresh-eyes verifier per final `confirmed`/`needs_validation` record before any report is produced, and restricts what a later "replacement" record may change without yet another independent re-verification; (6b) Phase 6 derives `REPORT.md`, `FINDINGS-DETAIL.md`, `NEEDS-VALIDATION.md` strictly from the final verified records, explicitly stating that an incomplete run must disclose unresolved candidates and "never end mid-phase" — it must reach one of exactly two terminal states (all artifacts written and both validators pass, or `run_status: "incomplete"` with an exact reason disclosed in the report).
**Enforcement:** Two executable Node.js CLI scripts, `validate-coverage-ledger.cjs` and `validate-findings.cjs`, each with a companion `*.test.cjs` Node test-runner suite. Both scripts read the target JSON file with no-follow/non-blocking file descriptors, reject symlinks/non-regular files/oversized input, enforce byte/nesting/array-count preflight limits before parsing, then run a hand-rolled JSON-Schema-like interpreter plus semantic checks (fingerprint format, sorted-fingerprint ordering, per-verdict allowed/forbidden fields, severity-cannot-exceed-impact, path-safety, coverage-ID canonical-form matching, state-machine invariants per ledger status, agent-ID format, attempt/archive ownership rules). On any error they `console.error` each message (capped at 100) and exit with code 1 (fail-closed); on success they print a `PASS:` line and exit 0. This is prose-plus-script enforcement: the workflow text ("Fix every structural and semantic error before continuing," "an invalid ledger cannot drive another assignment") is not itself machine-enforced — only running the validator against a JSON artifact is. Nothing in the skill enforces that the host agent actually invokes the validators, actually runs the sandboxed execution as described, or actually launches the described sub-agents; all of that depends on the calling agent's compliance with the prose instructions.
**Dependencies:** `node` (a JavaScript runtime) to run the two `.cjs` validator scripts and their tests; `gh` is listed as a runtime dependency by the facts table though no reference to it appears in the files read (possibly used by the host harness rather than by the skill's own instructions — see Could not determine). Depends on the host platform providing a Task-tool-like delegation mechanism for `research`/`general` sub-agents, and on the host environment being able to provide the described OS-enforced sandbox (no external network, empty allowlisted env, read-only mounts, resource limits) — the skill explicitly instructs the agent to not execute target code and to report a `needs_validation` blocker if that sandbox capability is missing.
**State it writes:** In full-audit mode only, under a new output directory (default `~/security-audit-skill/<repo-name>/run-<N>`, outside the target unless the user explicitly opts in and the parent verifies it's git-ignored): `run-metadata.json`, `architecture.md`, `coverage-ledger.json`, `findings.json`, `REPORT.md`, `FINDINGS-DETAIL.md`, `NEEDS-VALIDATION.md` (all writable only by the "parent"/coordinating agent), plus per-agent `agents/<agent-id>/scratch/` and parent-promoted `agents/<agent-id>/artifacts/` directories governed by an eleven-step no-follow/descriptor-based promotion procedure meant to prevent symlink/TOCTOU tricks during artifact promotion. In guidance mode it states it must not create an output directory or write audit artifacts.
**Fit with the bar:**
- Plan then stop before consequential work: supports it strongly — guidance mode explicitly withholds file creation and the full workflow unless the request is explicit; full-audit mode requires resolving output directory/source ref and a budget gate before any agent launches, and treats "if the request could mean either mode, ask one focused question before creating files or starting the complete workflow" as a rule.
- Executed evidence before "done": supports it strongly — the core principle "Use bounded local evidence," the mandatory sandboxed re-execution by an independent verifier in Phase 3, and the requirement that Phase 4/6 only proceed after both validators pass are all explicit "evidence-before-done" gates; a `confirmed` verdict requires nonempty `execution.observed_result` and the findings validator enforces this structurally.
- Say what was and was not checked: supports it strongly — the coverage ledger, `needs_validation` verdict class with mandatory blockers/validation plans, mandatory disclosure of `deferred`/`out_of_scope`/budget-incomplete states, and the Phase 6 report's required coverage-summary section are all built around stating what was and wasn't covered. One partial tension: nothing in the files enforces that the agent actually follows the prose workflow (only the two JSON artifacts get mechanically checked), so "say what was/wasn't checked" is enforced for artifact structure but not for whether the described process genuinely ran.
**What it does not cover:** The skill states its own limits explicitly: it is source- and sandboxed-local-only and refuses to assert live/deployed behavior, explicitly deferring proxy, provider, browser, identity, cloud, and packaging facts to `needs_validation`; it does not test availability against live/shared services, does not develop exploit/post-exploitation chains beyond the minimum boundary result, and does not modify target source (fixes are described, not applied). It does not itself provide or verify the sandbox, the delegation/sub-agent mechanism, or `gh`/`node` availability — it assumes the host platform supplies these and blocks itself (via `needs_validation`) when they are absent.

### Agent-directed text

- item-fd563265 (`SKILL.md` and identical blocks duplicated in `HUNTING.md` and `VALIDATION-AND-REPORTING.md`): "READ THE CODE AT DEPTH. Follow each assigned input through parsing, identity, authorization, normalization, state, derived copies, and the final sink... Stop at the smallest affected dummy record, wrong return value, process-integrity effect, or locally observable shared-resource effect."
- item-fd563265 (`SKILL.md`): "This skill is guidance by default. Loading it does not authorize the complete audit workflow or file creation."
- item-fd563265 (`AI-AND-LLM.md`, "Core discipline" block, explicitly meant to be pasted into sub-agent prompts): "Prompt injection alone is not a finding. Require a code-level boundary failure... A guardrail prompt is not a security boundary."
- item-fd563265 (`VALIDATION-AND-REPORTING.md`, candidate-verifier prompt): "You did not write this candidate. Try to refute it from repository source and bounded local evidence... Return exactly one JSON object and no surrounding prose."
- item-fd563265 (`HUNTING.md`, promotion procedure, marked "copy this promotion procedure verbatim into every hunter prompt"): the eleven-step no-follow, descriptor-based artifact promotion procedure.

(These are all *internal* instructions this skill tells its own coordinating/sub-agent to follow as part of its documented workflow, not instructions from an external/untrusted source — flagged here per instructions since they are agent-directed text embedded in the files.)

### Could not determine

- The facts table states a runtime dependency on `gh` (GitHub CLI), but no file content read (`SKILL.md`, the companions, `report-schema.json`, or either `.cjs`/`.test.cjs` script) references `gh` or any GitHub interaction; how or where `gh` is used is not shown in these files.
- Whether any hook, CI configuration, or external harness actually invokes `validate-findings.cjs`/`validate-coverage-ledger.cjs` automatically, versus the coordinating agent needing to run them itself as instructed in prose.
- Whether the host platform on which this skill runs actually provides the OS-enforced sandbox (no network, empty env, resource limits) and the no-follow/descriptor-based artifact-promotion primitives the skill assumes; the skill only describes what to do if these are unavailable, not how to verify their availability.
