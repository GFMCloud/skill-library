### Steelman Y

`item-4326747c` ("archify") is a mature, deeply engineered diagram-authoring skill with an actual CLI (`render/validate/deliver/compare/migrate/preview/check/visual-check/guide/brands/doctor/demo`), not just prose instructions. It enforces deterministic, fail-closed acceptance: `deliver` stages output privately, runs a battery of geometric/readability checks (finiteness, orthogonality, crossings, corridors, label clearance, route rhythm), and only atomically renames the candidate into place if every check passes — on failure it deletes staging state and "preserves the previous trusted artifact." It goes further than a code-only check by requiring real browser evidence: `bin/visual-check.mjs` drives headless Chrome over the DevTools protocol at four viewports/two themes and writes a JSON receipt, with an explicit rule that capture failures "must not be normalized to `skipped`." It bounds its own repair loop with a hard stop ("if two consecutive rounds do not improve that best count, stop and report the unresolved diagnostics truthfully"), requires a fixed receipt with SHA-256 hashes and independently reported deterministic vs. perceptual status, and its update-checker is a fully implemented, lock-protected, fail-closed state machine rather than a stub. Runtime dependencies are minimal and mostly already present (Node ≥18, no install required at runtime), and the optional Chrome dependency degrades gracefully to a documented skip rather than a hard failure.

### Steelman X

`item-fe76cb73` is compact and aimed precisely at the bar's first behavior: Step 0 requires the agent to pin down components, groupings, connections, and named flows in plain text and "reflect back" that list for user confirmation before any HTML is written when the description is loose — a plan-then-stop gate built into the workflow itself, not deferred to the surrounding harness. It treats build and QA as "one task, not two," backed by a concrete, cited real failure mode (a truncated file that looked finished but had nothing clickable), and its structural-integrity check is specifically designed to catch exactly that failure independent of cross-reference logic, failing closed with non-zero exit and FAIL lines. The exit-code gate to reach Step 8 is unambiguous, and the final step requires disclosing which architectural judgment calls were inferred, directly serving "say what was and wasn't checked." It is candid about its own limits — colors/fonts are disclosed as placeholders, branding is out of scope, and the optional screenshot dependency is flagged as not bundled rather than silently assumed.

### Scores

| Criterion | Y (item-4326747c) | X (item-fe76cb73) |
|---|---|---|
| Fit with the bar | 2 — Strong on evidence and disclosure, but the report itself flags a real gap: "no built-in mechanism to pause for explicit user sign-off before the final atomic `deliver` commit — acceptance is fully automatic once deterministic/browser checks pass," and "Artifact first... Do not plan exact coordinates in prose" discourages an up-front prose plan (Y "Fit with the bar," "What it does not cover"). | 3 — Step 0 explicitly requires listing components/flows and "reflect back... and confirm with the user before drawing if the description is loose," directly implementing plan-then-stop, plus a disclosure step at the end (X "What it makes the agent do," Step 0 and Step 8). |
| Enforcement mechanism | 3 — Executable CLI with fail-closed atomic delivery, geometric SVG checks, and headless-Chrome visual evidence explicitly barred from being "normalized to skipped" on failure (Y "Enforcement"). | 2 — `scripts/validate.py` fails closed on structural and cross-reference checks, but the optional screenshot/visual-evidence step "simply prints a notice and continues rather than failing" if its library is absent, so that portion of the gate "is not mechanically enforced" (X "Enforcement," "What it does not cover"). |
| Context cost | 3 — Counted facts given: 128-line SKILL.md, ~76-word trigger description, and a bounded "read exactly one schema... and one matching example" authoring path (Y "Trigger," "What it makes the agent do"). | 2 — Counted trigger description is larger (228 words) with no total SKILL.md line count given to compare against (X "Trigger"). |
| Maintenance burden | 3 — Node ≥18 only, "no install is required inside the skill package" at runtime; optional Chrome dependency degrades to a documented skip rather than failing (Y "Dependencies"). | 1 — Needs python3, a browser, and a browser-automation library "not bundled and must be fetched separately if absent"; counted facts also list `node` as a dependency the reviewed files don't otherwise explain, and a packaging step invokes an external module not provided (X "Dependencies," "Could not determine"). |
| Specificity | 3 — Enumerated concrete failure modes (finiteness, orthogonality, crossings, corridors, border runs, label clearance, route rhythm) and a concrete bounded stop condition on the repair loop (Y "Enforcement," "What it makes the agent do"). | 3 — Concrete 8-step checklist, a specific cited past failure (truncated file, nothing clickable) motivating the structural check, and an explicit stop instruction ("stop and go run Step 7 first") (X "Enforcement," "Agent-directed text"). |

### Per-item rows

| Item | Class | Note |
|---|---|---|
| item-4326747c | COMPLEMENT | Fills a gap in item-fe76cb73: robust, fail-closed enforcement of visual evidence (never normalizes capture failure to "skipped") and a lighter, more certain dependency footprint (no unbundled library, no unexplained extra runtime). |
| item-fe76cb73 | COMPLEMENT | Fills a gap in item-4326747c: an explicit plan-then-stop gate (Step 0 reflect-back-and-confirm) before any consequential diagram work begins, which item-4326747c's own report says it lacks before final delivery. |

### Deciding criteria

Fit with the bar (behavior 1, plan-then-stop) and Enforcement mechanism (fail-closed visual evidence vs. silent degrade) — X wins the first, Y wins the second, and neither dominates the other, which is why both are marked COMPLEMENT rather than one superseding the other.

### What I could not assess from reading alone

Neither report can confirm from static reading whether the described gates actually block an agent in practice: for Y, whether the host agent ever pauses for user approval before invoking `deliver` given the skill's own text discourages up-front prose planning; for X, what happens end-to-end when the screenshot library truly is absent (does the agent still claim "checked" for visual review, or accurately report it as unchecked?). Y's report also flags unresolved external dependencies (`gh` usage in release scripts, contents of several referenced build/test scripts) and X's flags an unexplained `node` dependency and a missing packaging module — both would need a live run to resolve. I did not form a view on which underlying product either report describes, so I have nothing to flag on that front.
