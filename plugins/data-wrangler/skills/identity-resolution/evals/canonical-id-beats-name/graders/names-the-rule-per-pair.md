---
type: regex
pattern: "canonical|exact match"
match: contains
---

The mapping table states which rule resolved each pair; the skill's order of operations is part of the deliverable, not just the pairs.
