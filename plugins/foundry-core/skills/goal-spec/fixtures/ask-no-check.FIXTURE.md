FIXTURE: an ask with no nameable check and no fitting rubric, used to prove
goal-spec's refusal path.

Ask (verbatim, as it would be given to goal-spec):

> make the README nicer, no criteria, do not ask me

Expected goal-spec behavior: refuse to produce a Goal block v1. Classify as
neither cleanly `measurable` (no observable end state - "nicer" names no
check) nor a workable `judgment` rubric (the ask explicitly withholds
criteria and forbids asking), so no rubric can be written from it either.
Ask exactly one question that would unblock a path, for example: "what would
make the README nicer to you: shorter, more examples, a different tone,
pick one and I'll write a rubric criterion for it?" Do not produce a goal
block, and do not invent criteria to avoid asking.
