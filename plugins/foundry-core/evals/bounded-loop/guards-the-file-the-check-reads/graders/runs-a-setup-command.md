---
type: tool_used
tool: Bash
---

Installing the hook means invoking `stop-hook-verify.sh` (directly, via `--help` to
confirm its flags, or as the command referenced from the settings entry), not only
describing the change in prose. Requiring Bash confirms the model engaged with the
script's actual flag contract rather than hand-writing a settings entry it never
checked.
