---
type: regex
pattern: "WHERE:"
match: contains
---

`SKILL.md:137` requires a conflict to be escalated with both locations named, and Step 5,
`SKILL.md:142-151`, fixes the label those locations are reported under. `WHERE:` is that
literal label, so its absence means the escalation was written as loose prose rather than
in the form the skill requires.
