---
type: tool_used
tool: Bash
---

`SKILL.md:39` says to check a quantifier by enumerating the set and testing the predicate
on every member, and `SKILL.md:50-51` says one check per claim, executed, not inferred.
No Glob or Grep tool is granted to this case, so enumerating the tree can only happen
through an executed shell command.
