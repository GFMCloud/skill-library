---
type: regex
pattern: "TRUTH:"
match: contains
---

Step 5, `SKILL.md:142-151`, requires four literal fields per defect: `CLAIM:`, `WHERE:`,
`TRUTH:` and `CHECK:`. `TRUTH:` is a fixed label the skill's own block mandates, so its
absence means the required format was not followed.
