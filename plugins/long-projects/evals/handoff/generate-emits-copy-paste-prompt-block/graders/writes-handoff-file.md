---
type: file_exists
path: "handoff-*.md"
---

The prompt block is handed over alongside a saved handoff file, never instead of
one (`SKILL.md:224-226`, `:242`). This grader keeps the case honest: a run that
prints a prompt block with no file behind it has not done Step 4.
