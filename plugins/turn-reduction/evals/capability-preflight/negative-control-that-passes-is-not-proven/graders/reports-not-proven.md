---
type: regex
pattern: "NOT PROVEN"
match: contains
---

The negative control succeeds, so the capability is NOT PROVEN however green the read and write were (requirement 2: the target must be capable of failing).
