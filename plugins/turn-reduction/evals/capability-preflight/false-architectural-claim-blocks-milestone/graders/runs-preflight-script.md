---
type: tool_used
tool: Bash
---

The verdict must come from actually running preflight.py against the saved manifest, not
from the model predicting that a `"blocking": false` entry is fine to ignore.
