---
type: regex
pattern: "Assumptions|assumptions"
match: contains
---

`SKILL.md:64` requires the state that could not be reached to land somewhere the user can
correct: "Say plainly what you couldn't verify, and record it as an explicit assumption in
the next section." That section is the labelled Assumptions block required by
`SKILL.md:72`, so the label is required output, not a phrasing the model may or may not
reach for.
