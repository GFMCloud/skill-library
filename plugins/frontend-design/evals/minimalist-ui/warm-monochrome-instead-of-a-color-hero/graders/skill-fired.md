---
type: tool_used
tool: Skill
---

Tests `SKILL.md:44` and `SKILL.md:54` (borders and dividers are exactly
`1px solid #EAEAEA`) and `SKILL.md:45-49` (accents are exclusively the four named
washed-out pastels), against the ban at `SKILL.md:26` on primary colored
backgrounds for large elements. The failure mode: "hero band plus status badges"
is the prompt that pulls a capable model straight to a saturated brand-color hero
and green/amber/red Tailwind badge chips. The rules only bind if the skill loaded,
so this grader establishes that first.
