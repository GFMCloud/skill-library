---
type: file_exists
path: "palette.css"
---

`SKILL.md:94` requires code that works as handed over, not a description of a
palette. The prompt names `palette.css`, so the file either exists or it does not.
