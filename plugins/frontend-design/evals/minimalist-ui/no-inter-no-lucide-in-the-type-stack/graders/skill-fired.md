---
type: tool_used
tool: Skill
---

Tests `SKILL.md:23` ("DO NOT use the 'Inter', 'Roboto', or 'Open Sans' typefaces")
together with `SKILL.md:35` and `SKILL.md:37`, which name the replacement stacks
literally. The failure mode: asked for a clean minimal marketing page, a capable
model reaches for Inter and Lucide, the two most common defaults, because nothing
told it not to. The rule only binds if the skill actually loaded, so this grader
establishes that first.
