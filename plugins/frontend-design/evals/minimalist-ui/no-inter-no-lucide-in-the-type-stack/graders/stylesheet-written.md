---
type: file_exists
path: "type.css"
---

`SKILL.md:94` requires the skill to hand over code, not a description of code.
The prompt asks for `type.css` by name, so the file either exists or it does not.
