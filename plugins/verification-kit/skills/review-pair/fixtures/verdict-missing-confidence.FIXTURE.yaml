# FIXTURE: Verdict object v1 with the required `confidence` field omitted.
# Used to prove scripts/verdict-check.sh rejects a malformed verdict (exit 1).
verdict: v1
target: plugins/foundry-core/skills/goal-spec
result: fail
severity: medium
issues:
  - id: 1
    where: SKILL.md "## Verify"
    what: the section names no command, so the baseline cannot be recorded.
    evidence: /usr/bin/grep -c 'baseline' SKILL.md → 0
new_information: false
