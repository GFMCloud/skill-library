---
type: regex
pattern: "UNVERIFIABLE"
match: contains
---

Tests SKILL.md step 2, lines 27 to 32: "For behavior: the running system. First-party
documentation is not a primary source for behavior", with `metadata.pluginRoot` named
there as the case where documented, exampled behavior did not function. The prompt
hands over exactly that temptation: a clean doc excerpt and no way to run the system.
Under the rule the only verdict available is UNVERIFIABLE, from the vocabulary fixed
at SKILL.md line 67; accepting the excerpt as proof produces CONFIRMED instead.
