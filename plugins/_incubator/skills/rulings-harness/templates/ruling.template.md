---
slug: <slug>
category: ruling            # ruling | burn-derived
revisit-by: <YYYY-MM-DD>    # burn-derived rulings still get a date, for the incident-link sanity check, even though the ruling itself does not expire
status: active
---

# <One imperative line: the ruling itself>

## Evidence

`<What was measured, when, by what method. For burn-derived rulings, this is the
incident: what happened, when, and where the fuller account lives (retro doc, commit,
postmortem).>`

## Falsifier

`<The specific, checkable condition that would overturn this ruling. "none,
burn-derived" only if category is burn-derived; every plain ruling must have a real
one.>`

## Re-test

`<The exact fixture or command that reproduces the measurement. Must be runnable as
written, not a description of a process.>`
