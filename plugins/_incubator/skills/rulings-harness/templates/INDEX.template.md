# Rulings index: <LOCATION-NAME>

One row per file in this directory. This is the enumeration; nothing else restates
the count or the list. Regenerate this table any time a ruling is added, retired, or
re-decided: do not hand-count rulings elsewhere.

| Slug | Ruling (one line) | Category | Revisit-by | Status |
|---|---|---|---|---|
| `<slug-1>` | `<one-line imperative>` | ruling / burn-derived | `<YYYY-MM-DD>` | active |

Status values: `active`, `needs-redecision` (a `/rulings check` pass flagged it),
`superseded` (kept for history, no longer binding: link the replacement).
