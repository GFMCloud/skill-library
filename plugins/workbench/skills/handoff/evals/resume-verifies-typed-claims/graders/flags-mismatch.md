---
type: regex
pattern: "mismatch"
match: contains
---
The mismatch fixture's branch claim is wrong, so the response must call out at least
one mismatch and stop to ask rather than silently proceeding.
