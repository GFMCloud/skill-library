## This pass is labeled, not context-clean

This slot is a challenge to the bar itself. One of the two candidates was written to the
three behaviors above, so that candidate is expected to fit them, and fitting them is not
by itself a reason to prefer it. Judge whether the other candidate does the same job
better, worse, or differently, on the content of the reports. Your rows here are recorded
separately and change no other slot.
