---
type: tool_used
tool: Bash
---
Resume Mode requires re-running each claim's `check` command against the live artifact,
so a Bash call is expected rather than a claim taken on the document's word alone.
