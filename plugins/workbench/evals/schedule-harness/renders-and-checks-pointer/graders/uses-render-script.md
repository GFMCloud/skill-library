---
type: tool_used
tool: Bash
min: 1
---

Rendering the pointer must go through scripts/render-pointer.py rather than the model
writing the pointer body freehand, so the rendered file always traces to the one
editable template.
